# Governance

Governance is how Zintrust projects keep quality and security consistent as the codebase grows.

## Quality gates

Typical gates include:

- Type-checking
- Linting
- Tests
- Optional static analysis (Sonar)

This repo provides a CLI command that runs a full QA suite and emits a report.

See also: [linting-ci-gates](linting-ci-gates)
