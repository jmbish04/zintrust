# CLI Governance

The Zintrust CLI is designed to keep teams aligned on:

- naming
- file layout
- default patterns for routes/controllers/middleware

## QA workflow

Use `zin qa` locally and in CI to standardize quality gates.

## Scaffolding

Prefer the CLI generators over hand-rolling files so templates stay consistent.
