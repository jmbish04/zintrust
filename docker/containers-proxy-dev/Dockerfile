# syntax=docker/dockerfile:1.6
# Thin wrapper so Wrangler can build from a Dockerfile path (required), while
# letting you control when the heavy base image is rebuilt.

FROM zintrust-containers-proxy:local-amd64
